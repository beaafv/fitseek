
<% content_for :meta_title, "#{@workout.name} is on #{DEFAULT_META["meta_product_name"]}" %>
<% content_for :meta_description, @workout.description %>
<% content_for :meta_image, cl_image_path(@workout.photo.key) if @workout.photo.attached? %>

<%= render "shared/navbar2" %>
<%= render "shared/show" %>


  <div class="mt-5"style="width: 100vw; height: 25vh"
          data-controller="map"
          data-map-markers-value="<%= @markers.to_json %>"
          data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
   </div>



<%= render "shared/navbar" %>
